# Story: T52 — ML Dropout Pipeline (T52a–d)

**Lane:** high-risk  
**Owner:** Hoàng *(reassign 24/06 — giữ task ID `T52*`)*  
**Sprint:** Sprint 3 · Gate G3-3 (T52d) + G3-4 (V20)  
**Depends on:** H46, T50, H52

### Read first

- [Sprint3.md](../Sprint3.md) — T52 rows, bootstrap, label rules
- [H46-Implementation.md](../H46-Implementation.md)
- [docs/decisions/0006-ml-agent-boundary.md](../../decisions/0006-ml-agent-boundary.md)
- [docs/decisions/0004-pgvector-over-chromadb.md](../../decisions/0004-pgvector-over-chromadb.md)

### Scope (tuần tự)

| Task | Deliverable | Paths |
|:-----|:------------|:------|
| **T52a** | Labels `expelled`/`withdrawn` (~120 SV); fix `is_active` on import | `scripts/import_academic_dataset.py` |
| **T52b** | DWH feature view (GPA, fail rate, cohort…) | `app/analytics/etl.py`, `dwh` views |
| **T52c** | Train classifier → `ml.model_run`; `/admin/ml/train` | `app/ml/*` |
| **T52d** | Dropout-risk API + RBAC; agent tool **reads** `ml` (explain only) | API + `app/agent/tools.py` |

**H25a** (feature spec) sau T52a verify · **H25b** (eval report) sau T52d.

### Label rules

- Positive: `status IN ('expelled', 'withdrawn')`
- Negative: `status = 'active'`, cohort ≤ 2023
- Train on `status`, not `is_active`

### Do not touch

- `frontend/` (V20 owner Hiếu — consume T52d API only)
- Agent graph routing topology (H48) — T52d adds tool, no edge changes
- H49–H51, T49 (Sprint 4)

### Verify

```powershell
# After T52a
docker compose exec db psql -U eduinsight -d eduinsight -c "SELECT status, COUNT(*) FROM students GROUP BY status ORDER BY status;"

cd backend
ruff check .
pytest -v
```

### Done when

- [x] T52a: ~120 dropout labels in DB; `is_active` reflects status
- [x] T52b: DWH features materialized for training
- [x] T52c: model run stored in `ml`; train endpoint works
- [x] T52d: dropout-risk API + RBAC; agent explains `ml` predictions (ADR-006)
- [x] Sprint3 T52a–d checkboxes updated

### Notes

Hưng handed off ML implementation 24/06; remains RC/DevOps support. Bootstrap commands in Sprint3.md under Hoàng section.
