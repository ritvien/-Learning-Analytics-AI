# Story: V42 — Default Semester Filter

**Lane:** normal  
**Owner:** Hiếu  
**Sprint:** Sprint 3  
**Depends on:** T43 (semester metadata API)  
**Status (26/06):** ✅ Done

### Read first

- [Sprint3.md](../Sprint3.md)
- Analytics filter endpoints — `backend/app/api/v1/endpoints/analytics.py`

### Scope

Các trang có filter kỳ học (dashboard, report, analytics…) **mặc định** chọn kỳ học mới nhất / hiện tại từ API (`semesters`, sort `year`+`term`). User **vẫn đổi được** filter; selection persist qua `sessionStorage` (`vinuni_selected_semester`) hoặc URL query.

**Implemented in:** `analytics/page.tsx`, `analytics/sections/page.tsx` (và các trang analytics liên quan).

### Do not touch

- Backend semester schema (T43 owner nếu cần API mới)
- RBAC (V41)

### Verify

```powershell
cd frontend
npm run lint
npm test
```

Manual: mở dashboard → semester pre-selected = mới nhất; đổi semester → data refresh đúng.

### Done when

- [x] Tất cả trang có semester filter default = latest/current
- [x] User có thể chọn kỳ khác; selection persist trong session (`sessionStorage` / URL query)
- [x] Không regression Playwright smoke (V39)
