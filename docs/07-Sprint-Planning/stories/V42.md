# Story: V42 — Default Semester Filter

**Lane:** normal  
**Owner:** Hiếu  
**Sprint:** Sprint 3  
**Depends on:** T43 (semester metadata API)

### Read first

- [Sprint3.md](../Sprint3.md)
- Analytics filter endpoints — `backend/app/api/v1/endpoints/analytics.py`

### Scope

Các trang có filter kỳ học (dashboard, report, analytics…) **mặc định** chọn kỳ học mới nhất / hiện tại từ API (`semesters`, `current` hoặc sort `year`+`term`). User **vẫn đổi được** filter; không khóa UI.

### Do not touch

- Backend semester schema (T43 owner nếu cần API mới)
- RBAC (V41)

### Verify

```powershell
cd frontend
npm run lint
npm test
```

Manual: mở dashboard → semester pre-selected = mới nhất; đổi semester → data refresh đúng.

### Done when

- [ ] Tất cả trang có semester filter default = latest/current
- [ ] User có thể chọn kỳ khác; selection persist trong session (URL query hoặc state)
- [ ] Không regression Playwright smoke
