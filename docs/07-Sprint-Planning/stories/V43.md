# Story: V43 — Superadmin Observability UI

**Lane:** normal  
**Owner:** Hiếu  
**Sprint:** Sprint 3  
**Depends on:** T53 ✅, V41 ✅  
**Status (26/06):** ⬜ Not started — T53 API sẵn sàng; chưa có page trong `frontend/`

### Read first

- [docs/19-User-Behavior-Observability/README.md](../../19-User-Behavior-Observability/README.md)
- [Sprint3.md](../Sprint3.md)
- [T53.md](./T53.md) — API contract

### Scope

Trang **superadmin** quan sát hành vi user tổng quan:

- Danh sách browser **sessions** (`session_id`, user, thời gian)
- **Event log** — filter theo user, event_name, trace_id, khoảng thời gian
- **Trace drill-down** — nối page → chat → agent run → tool (theo `trace_id`)
- Metric tổng quan: số event, error rate, top routes/modules (từ API T53)

Chỉ role `superadmin` thấy nav/route; các role khác 403 hoặc ẩn menu.

### Do not touch

- `obs.event_log` schema / `log_event()` (T41/T53)
- Agent graph (Hoàng)

### Verify

```powershell
cd frontend
npm run lint
npm test
```

Login `superadmin@epu.edu.vn` → `/manager/observability` → list sessions + events + user aggregates.

**API client:** thêm methods trong `frontend/src/lib/api.ts` gọi `GET /api/v1/observability/admin/*` (hiện chỉ có client `POST /observability/events` cho ingestion).

### Done when

- [ ] Superadmin-only route + nav entry
- [ ] Session list + event log table với filter cơ bản
- [ ] Trace detail view (ít nhất request_id / trace_id chain)
- [ ] Overview cards/metrics từ T53 aggregates
- [ ] Non-superadmin không truy cập được
